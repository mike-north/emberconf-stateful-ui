import { markdown } from 'pull-request/helpers/markdown';
import { module, test } from 'qunit';

module('Unit | Helper | markdown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = markdown([42]);
  assert.ok(result);
});
